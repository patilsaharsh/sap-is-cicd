name: Module 2 - Authentication

on:
  workflow_dispatch:

jobs:
  auth:
    runs-on: ubuntu-latest
    environment: DEV

    steps:
      - uses: actions/checkout@v4

      - name: Test Connection
        run: |
          echo "GitHub Action is running successfully."
